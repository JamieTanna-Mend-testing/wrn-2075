# Podfile — exercises the `pod` datasource (CocoaPods registry).
# Renovate looks up pod versions against api.github.com for spec repos.
platform :ios, '17.0'

target 'ExampleApp' do
  use_frameworks!
  pod 'Alamofire', '~> 5.10'
  pod 'SwiftyJSON', '~> 5.0'
end
