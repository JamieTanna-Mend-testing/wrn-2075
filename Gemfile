source 'https://rubygems.org'

gem 'rails', '~> 8.0'
gem 'rspec', '~> 3.13'

# Gem sourced from GitHub Package Registry — exercises the `rubygems` datasource
# with matchHost=github.com. Renovate needs the GitHub token for this source
# because rubygems.pkg.github.com requires authentication even for public packages.
source 'https://rubygems.pkg.github.com/github' do
  gem 'github-ds', '0.5.4'
end
